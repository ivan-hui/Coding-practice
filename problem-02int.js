var buildTree = function(preorder, inorder) {
    //i:2 arrays
    //o:binary tree
    
    //recursive function
        //find out first preorderarray that matches with inorder
            
    const recurse = function(preorder, inorder) {
        //find first number in preoder that is in inorder
        let root = preorder[0]
        
        //build the node
        
        //recurse left node with number left of inorder
        //recurse right node with number right of inorder
        
    }
};